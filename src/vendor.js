/* This file contains references to the vendor libraries
so they will be catched separated because they dont change a lot,
a vendor library changes are less frequent */

import fetch from 'whatwg-fetch';
